@if ((Model == null) || (Model.Count == 0))
{
<div class="card">
    <div class="card-content">
        <span class="card-title center"><strong>Ancora nessuna Tipologia di Lezione creata</strong></span>
        <br />
        <p>Sembrerebbe che al momento non siano state ancora definite delle Tipologie di Lezioni.</p>
        <br />
        <p>Inizia ad inserirne almeno una per poterle successivamente pianficare.</p>
        <br />
        <a asp-action="LezioneAdd" asp-controller="TipologieLezioni" class="btn-floating halfway-fab waves-effect waves-light red"><i class="material-icons">add</i></a>
    </div>
    @*<div class="card-action">
        <a href="#">This is a link</a>
    </div>*@
</div>
}

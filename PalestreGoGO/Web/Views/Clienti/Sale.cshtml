@model List<LocationViewModel>
@{
    ViewData["Title"] = "Sale";
    Layout = "_LayoutClienti";
}
@Html.Partial("_MenuCliente", ViewData["IdCliente"])
<div class="container">
    <div class="row">
        <div class="col s8">
            <span class="section-header">
                Sale
            </span>
        </div>
        <div class="col s4">
            <a class="btn-floating btn-large waves-effect waves-light red" asp-action="SalaEdit" asp-route-id="-1" data-position="bottom" data-tooltip="Aggiungi Sala" title="Aggiungi Sala"><i class="material-icons">add</i></a>
        </div>
    </div>
    <div class="row">


        @if ((Model == null) || (Model.Count == 0))
        {
            <span>Nessuna lezione inserita.</span>
        }
        else
        {
            <table class="stripped responsive-table">
                <thead>
                    <tr>
                        <th>Sala</th>
                        <th>Descrizione</th>
                        <th>Capienza</th>
                        <th>Azioni</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var s in Model)
                    {
                        <tr>
                            <td data-id-sala="@s.Id">@s.Nome</td>
                            <td>@s.Descrizione</td>
                            <td>@s.CapienzaMax</td>
                            <td>
                                <div><a asp-action="SalaEdit" asp-controller="Clienti" asp-route-id="@s.Id">Modifica</a></div>
                                <div><a asp-action="Saladelete" asp-controller="Clienti" asp-route-id="@s.Id">Elimina</a></div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</div>
@section Scripts{
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            //var tooltipOptions =
            var elems = document.querySelectorAll('.tooltipped');
            var instances = M.Tooltip.init(elems);
        });
    </script>
}
